---
title: sample note
description: here we are
date: "2025-04-22"
---

# Heading 1 here

link to: [MDX](https://mdxjs.com "title")

blockquote:

> A greater than… everything else

code:

Some `backticks` for inline.

// Example TypeScript function within an MDX code block

```typescript
interface User {
  id: number;
  name: string;
  isAdmin: boolean;
}

/**
 * Greets a user.
 * @param user - The user object.
 * @returns A greeting string.
 */
function greetUser(user: User): string {
  if (user.isAdmin) {
    return `Hello, Admin ${user.name}! Welcome back.`;
  }
  return `Hello, ${user.name}!`;
}

// Example usage:
const regularUser: User = { id: 1, name: 'Alice', isAdmin: false };
const adminUser: User = { id: 2, name: 'Bob', isAdmin: true };

console.log(greetUser(regularUser)); // Output: Hello, Alice!
console.log(greetUser(adminUser));   // Output: Hello, Admin Bob! Welcome back.

// You can embed this block directly into your .mdx file.
// MDX processors will typically handle syntax highlighting.
```

Image
![Alt text](/images/meme.png "my photo")
<img src="/images/meme.png" alt="Roadmap diagram" width="600" height="400" />

***
break:

list - unordered

* asterisks for unordered items
* asterisks for unordered items
* asterisks for unordered items
* asterisks for unordered items

1. decimals and a dot for ordered items
1. decimals and a dot for ordered items
1. decimals and a dot for ordered items